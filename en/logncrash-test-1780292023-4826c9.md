## Log & Crash Reporting Test

This document is a short Korean sample to verify that the translation pipeline properly sends logs to Log & Crash Search after completing work.

It verifies whether a single line log containing fields like `jobId`, `sourcePrUrl`, `translationPrUrl`, `longDurationSec` reaches the collection server when the job finishes, rather than the quality of the translation result.