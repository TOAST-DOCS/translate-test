## Log & Crash Reporting Test

This document is a short Korean sample to verify that the translation pipeline successfully sends logs to Log & Crash Search after a job is completed.

It validates not the quality of the translation output, but whether a single-line log containing fields such as `jobId`, `sourcePrUrl`, `translationPrUrl`, and `longDurationSec` reaches the collector server when the job ends.