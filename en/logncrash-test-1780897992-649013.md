## Log & Crash Reporting Test

This document is a short Korean sample to verify that the translation pipeline properly transmits logs to Log & Crash Search after job completion.

It verifies whether a single-line log containing fields such as `jobId`, `sourcePrUrl`, `translationPrUrl`, and `longDurationSec` reaches the collection server when the job completes, not the quality of the translation results.