<!-- machine_translated: true -->

<!-- pre-align:aligned sig=3deff3dd96d2 -->

<a id="foundry.console.guide"></a>
## Machine Learning > NHN Cloud Foundry > コンソールユーザーガイド { #foundry.console.guide }

このドキュメントでは、NHN Cloud Foundry コンソールでデータソース、パイプライン、分析（クエリ・チャート・ダッシュボード）、アプリを管理する方法を説明します。

設定項目の表の必須列は次を意味します。

- `O`: 必須入力項目
- `X`: 任意項目
- `O*`: 他の設定値によって必須かどうかが変わる項目

画面に表示される日付と時刻は、すべてアクセスしたブラウザのタイムゾーンを基準としています。

<a id="status"></a>
## 現況 { #status }

コンソールパス: **Machine Learning > NHN Cloud Foundry > 現況** タブ

現況タブでサービスの有効化状態とテナント設定を確認します。サービスを使用するには、まずこのタブで有効化状態を確認し、有効化されていない場合は有効化を申請する必要があります。

<a id="status.activate"></a>
### サービス有効化申請 { #status.activate }

サービスの有効化はコンソールから直接実行することはできません。[1:1 お問い合わせ](https://www.nhncloud.com/kr/support/inquiry)に希望するリソースサイズを含めてお問い合わせください。申請したクラスターが作成されると、データソースの作成から順番に使用できます。

リソースサイズ別に使用できる機能は次のとおりです。

| 値 | 説明 |
| --- | --- |
| SMALL | 基本リソース: データソース、共通機能、チャートクエリを使用可能 |
| MEDIUM | 基本 + データパイプラインを追加で使用可能 |
| LARGE | 基本 + AI アプリを使用可能 |
| XLARGE | すべての機能を使用可能（データパイプライン + AI アプリ） |

<a id="status.info"></a>
### サービス状態の確認 { #status.info }

有効化後は、現況タブで次の情報を確認できます。

| 項目 | 説明 |
| --- | --- |
| サービス状態 | 現在のサービスの有効化状態 |
| テナントドメイン | サービスにアクセスする際に使用するドメイン |

- 有効化後にリソース変更が必要な場合も、1:1 お問い合わせで申請します。
- サービス環境の構成または整理中は、進行状況が合わせて表示されます。
- **[無効化]** ボタンをクリックすると、サービスを無効化できます。

!!! danger "注意"
    サービスを無効化すると、作成されたすべてのリソースが削除され、この操作は元に戻すことができません。

<a id="datasource"></a>
## データソース { #datasource }

コンソールパス: **Machine Learning > NHN Cloud Foundry > データソース** タブ

データソースは、NHN Cloud Foundryで分析するデータを保存する単位です。データソースの作成、照会、削除はコンソールで行います。

!!! danger "注意"
    本サービスをご利用の際は、個人情報を含む情報を入力しないようにご注意ください。
    本サービスは、お客様が入力する個人情報に対する別途のセキュリティ対策を提供していないため、個人情報を含む情報の入力および保存はお控えください。

<a id="datasource.list"></a>
### データソース一覧 { #datasource.list }

データソース一覧画面では、次の情報を確認できます。

| カラム | 説明 |
| --- | --- |
| タイプ | データソースの種類 |
| データソース名 | データソースを識別する名前 |
| テーブル名 | データが保存されるテーブル名 |
| 状態 | データソースの現在の状態 |
| データ量 | ロードされたデータの行数 |
| 作成日 | データソースが作成された日時 |
| 詳細 | 虫眼鏡アイコンをクリックすると詳細情報を照会 |

タイプ:

| 値 | 説明 |
| --- | --- |
| ファイル | アップロードしたCSVファイルで作成したデータソース |
| Prometheus API | 収集 API でメトリクス (時系列) データを受け取るデータソース |
| レコメンド | レコメンド結果が保存されるデータソース |
| 単変量異常検知結果 | 単変量異常検知結果が保存されるデータソース |
| データセット | パイプラインが作成したデータソース |

状態:

| 値 | 説明 |
| --- | --- |
| INITIALIZING | データソースの初期化中 |
| PROCESSING | データ処理中 |
| INGESTING | データロード中 |
| COMPLETED | データソースの処理完了 |
| FAILED | データソースの処理失敗 |
| DELETING | データソースの削除中 |

タイプがファイルのデータソースは、最新のファイルアップロード結果が状態の横にアイコンで合わせて表示されます。

| 値 | 説明 |
| --- | --- |
| 反映中 | 最近アップロードしたファイルを反映中 |
| 確認推奨 | 最近アップロードしたファイルは反映されましたが、確認が必要な項目があります。データソースの詳細で内容を確認してください |
| 確認必要 | データソースは使用可能ですが、最近アップロードしたファイルは未反映です。ファイルを確認後、再度アップロードしてください |

- ステータスバッジ横の疑問符アイコンにマウスを合わせると、ステータスの説明が表示されます。
- 上部の検索機能で、データソース名またはIDを基準にフィルタリングできます。
- タイプ・データソース名・テーブル名・状態カラムは、カラムヘッダーのフィルターでも絞り込みができます。
- ページあたりの表示件数を調整できます（10件、20件、50件 / デフォルト10件）。

!!! tip "ヒント"
    タイプが推薦、単変量異常検知結果、データセットの項目は、ユーザーが直接作成することはできません。推薦と単変量異常検知結果はアプリを作成するとき、データセットはパイプラインを実行するときに自動的に作成されます。

<a id="datasource.create"></a>
### データソースの作成 { #datasource.create }

**[データソース作成]** ボタンをクリックすると、作成モーダルが開きます。作成モーダルは基本設定、接続設定、詳細設定で構成されており、接続設定で選択したデータソースのタイプによって、スキーマの入力方式と詳細設定の項目が異なります。設定が完了したら **[追加]** ボタンをクリックすると、データソースが作成されます。

<a id="datasource.create.basic"></a>
#### 基本設定 { #datasource.create.basic }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| データソース名 | O | 韓国語、日本語、英字、数字、スペース、-、_ が使用可能（1〜64文字） |
| テーブル名 | O | 英小文字、数字、_ が使用可能（1〜64文字）。数字で始めることはできません。SQL予約語は使用できません |
| 説明 | X | データソースの説明 |

既に使用中のデータソース名とテーブル名は使用できません。

<a id="datasource.create.connection"></a>
#### 接続設定 { #datasource.create.connection }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| データソースタイプ | O | データを積載する方式を選択します。デフォルトはファイルアップロード |

選択できるタイプは次のとおりです。

| 値 | 説明 |
| --- | --- |
| ファイルアップロード | CSV ファイルをアップロードしてデータを積載します。作成後は CSV または Ingest API でデータを更新します。 |
| Prometheus API | 指標データを収集 API でリアルタイムに積載します。単変量異常検知アプリの入力として使用します。 |

<a id="datasource.create.detail"></a>
#### ファイルアップロードの詳細設定 { #datasource.create.detail }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| CSVファイル | X | **[ファイルを選択]** ボタンでアップロードするCSVファイルを選択します（最大100MB） |
| ヘッダー設定 | X | **[最初の行はヘッダーです]** にチェックを入れると、CSVの1行目をカラム名として使用します |
| 主キーフィールド | X | 行を識別するカラムをドロップダウンで指定します |
| スキーマ | O | データのフィールド名とタイプの定義 |

主キーフィールドは次のように指定します。

- CSVファイルを選択すると、最初のカラムでドロップダウンが自動的に作成されます。ドロップダウンをクリックして他のカラムに変更します。
- **[主キーを追加]** ボタンでドロップダウンを追加すると、2つ以上のカラムをまとめて指定できます。既に選択したカラムは、他のドロップダウンの候補に表示されません。
- ドロップダウンをすべて削除すると、主キーが未指定の状態になります。
- ヘッダーがないファイルは、カラムが順番と最初のデータ行の値で表示されます。

スキーマは次の方法で入力します。

- タイプ推論: CSVファイルを選択した後、**[タイプ推論]** ボタンをクリックすると、CSVの先頭サンプル（最大1,000行）からカラムタイプを推論し、スキーマ入力欄を自動で埋めます。誤って推論されたタイプはユーザーが直接修正します。
- テーブル編集: **[フィールドを追加]** ボタンで行を追加し、各行にフィールド名とタイプを指定します。備考欄にはタイプ推論の結果のうち確認が必要な項目が表示されます。
- JSON編集: **[JSONで直接編集]** ボタンをクリックし、以下の形式のJSON配列を直接入力します。

スキーマJSON形式の例:

```json
[
  { "fieldName": "hostname", "fieldType": "string" },
  { "fieldName": "portName", "fieldType": "string" },
  { "fieldName": "trafficIn", "fieldType": "double" },
  { "fieldName": "eventTimestamp", "fieldType": "timestamp" }
]
```

サポートするフィールドタイプ:

| 値 | 説明 |
| --- | --- |
| boolean | ブール値 |
| int | 整数（32bit） |
| long | 整数（64bit） |
| float | 実数（32bit） |
| double | 実数（64bit） |
| string | 文字列 |
| timestamp | タイムスタンプ |
| datetime | 日時（YYYY-MM-DD HH:MM:SS） |
| date | 日付 |
| array | 配列（基本） |
| array&lt;double&gt; | double配列 |
| array&lt;int&gt; | int配列 |
| array&lt;string&gt; | string配列（レコメンドモデル用） |
| array&lt;float&gt; | float配列（レコメンドモデル用） |

!!! danger "注意"
    予約フィールド名 `system_eventTimestamp` は使用できません。

<a id="datasource.create.detail.prometheus"></a>
#### Prometheus API 상세 설정 { #datasource.create.detail.prometheus }

Prometheus API タイプは、収集 API が送信するレコードの形式が決まっているため、スキーマを直接入力しません。基本設定のスキーマの箇所には、次の固定スキーマが読み取り専用の表として表示され、このスキーマのままテーブルが作成されます。

| フィールド名 | データ型 | 説明 |
| --- | --- | --- |
| timestamp | timestamp | メトリクスの時刻。ミリ秒 epoch |
| value | double | 測定値 |
| labels | array&lt;struct&gt; | メトリクスに付与されたラベル全体 |
| labelHash | string | 自動的に計算されるシリーズ識別子 |
| groupHash | string | 自動的に計算されるグループ識別子 |
| metadata | map | 送信されたままの形で保存される付加情報 |

システムカラム `eventTimestamp`、`ingestTimestamp` は自動的に追加されます。

詳細設定では、次の 2 項目を指定します。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| シリーズ識別ラベル | O | 1 つの時系列をどのラベルの組み合わせで区別するかを指定。**[全ラベルを使用]** または **[直接指定]** から選択し、デフォルトは **[全ラベルを使用]** |
| グループラベル | X | 時系列をまとめて管理する単位。指定したラベル値が同じ時系列が 1 つのグループになる。空欄にするとデータソース全体が 1 つのグループになる |

- シリーズ識別ラベル: 時系列ごとに個別に学習および推論します。**全ラベル使用**はラベルの組み合わせが異なる場合、別の時系列として扱います。**直接指定**は指定したラベル値が同じデータのみを1つの時系列としてまとめ、残りのラベルは区別に使用しません。
- グループラベル: たとえばルールIDラベルを指定すると、ルール値ごとに1つのグループが作成されます。空白のままにすると、データソース全体が1つのグループになります。グループはメトリクスが入力された時点で登録されます。
- ラベル名は英字または `_` で始まり、英字、数字、`_` のみ使用できます。複数のラベルはカンマ区切りで入力し、同じラベルを2回入力することはできません。
- シリーズ識別ラベルを**直接指定**した場合に、グループラベルがそのリストに含まれていないと警告が表示されます。グループが異なっても同じ時系列として統合される可能性があるため、シリーズ識別ラベルにも含めることをお勧めします。

!!! tip "注記"
    Prometheus API データソースにデータを送信する方法については、詳細表示の **[収集方法]** タブまたは [APIガイド](../api-guide/#metrics.ingest.api) の「メトリクス収集」を参照してください。

<a id="datasource.delete"></a>
### データソースの削除 { #datasource.delete }

一覧から削除するデータソースをチェックボックスで選択し、**[削除]** ボタンをクリックします。

- 削除するとテーブルとロードされたデータも一緒に削除されます。
- 収集作業が進行中の場合、削除できません。

<a id="datasource.detail"></a>
### 詳細表示 / プレビュー { #datasource.detail }

- **[詳細]**（虫眼鏡アイコン）: データソースの情報を確認します。
- **[プレビュー]**（⌄アイコン）: ロードされたデータをテーブル形式でプレビューします。

詳細表示は次のタブで構成されます。

| タブ | 説明 |
| --- | --- |
| 接続情報 | データソースID、データソース名、テーブル名、タイプ、説明、状態 |
| カタログ | フィールド名とデータ型の一覧照会、**[フィールドを追加]** |
| イベント設定 | タイプがファイルのデータソースにのみ表示。Event APIの有効化状態の確認と切り替え |
| 収集方法 | タイプが Prometheus API のデータソースにのみ表示されます。メトリクスを送信する方法のガイドです。 |

タイプが Prometheus API のデータソースは、接続情報タブに**シリーズ識別ラベル**と**グループラベル**が一緒に表示されます。値を指定していない場合は、それぞれ「全ラベルを使用」「単一グループ（データソース全体）」と表示されます。

<a id="datasource.detail.event"></a>
#### イベント設定 { #datasource.detail.event }

Event API は、リアルタイムイベントデータを HTTP API で収集する機能です。イベント設定タブのトグルで有効化および無効化します。

| 状態 | 説明 |
| --- | --- |
| OFF | イベント収集が無効化された状態 |
| 有効化中 | イベント収集に必要なリソースを作成中。完了すると自動的に状態が更新されます |
| ON | イベント収集が有効化された状態 |
| 失敗 | 有効化に失敗した状態。トグルを再度クリックしてリトライします |

- データソースが `COMPLETED` 状態である必要があります。それ以外の状態では、**設定不可**の案内とともに現在の状態が表示されます。
- 有効化は非同期で進行し、進捗状況は画面が自動的に更新して表示されます。

!!! danger "注意"
    Event API を有効化すると、CSV ファイルのアップロードと Ingest API のスナップショットアップロードが遮断されます。スナップショットをアップロードするには、Event API を先に無効化する必要があります。
    有効化状態では、データソースのスキーマ変更も制限されます。カタログにフィールドを追加するには、Event API を先に無効化する必要があります。
    スナップショットアップロード後に Event API を再度有効化すると、イベント収集位置が最新のオフセットに初期化されます。

イベントを送信する方法については、[APIガイド](../api-guide/#event.ingest.api)の「イベント収集」を参照してください。
有効化と無効化は、[APIガイド](../api-guide/#event.ingest.api.enable)の「Event API 有効化・無効化」でも行えます。

<a id="datasource.detail.ingest"></a>
#### 収集方法 { #datasource.detail.ingest }

タイプが Prometheus API のデータソースにメトリクスを送信する方法を案内します。各項目は **[コピー]** ボタンでコピーできます。

| 項目 | 説明 |
| --- | --- |
| エンドポイント | メトリクスを転送する API パス |
| リクエストヘッダ | このデータソースのアプリキーが設定された X-NC-APP-KEY ヘッダ |
| リクエスト本文の例 | このデータソースのグループラベルが反映されたリクエスト本文の例 |
| ルール | 時刻単位、ラベル、付加情報、値、レスポンスに関する注意事項 |

!!! danger "注意"
    リクエストヘッダ項目にはアプリキーのみ表示されます。メトリクスを送信する際は、認証トークンヘッダである `X-NHN-Authorization` も一緒に送信する必要があり、アプリキーのみ送信するとリクエストが拒否されます。

詳細なリクエスト形式については、[APIガイド](../api-guide/#metrics.ingest.api)の「メトリクス収集」を参照してください。

<a id="datasource.edit"></a>
### データの更新 { #datasource.edit }

データソース名とテーブル名は、作成後に変更できません。ロードされたデータは新しいCSVファイルで更新し、フィールドは追加できます。

<a id="datasource.edit.csv"></a>
#### CSVによるデータ修正 { #datasource.edit.csv }

一覧でタイプがファイルのデータソース行のその他（⋯）メニューで **[CSVで修正する]** をクリックします。

1. 既存のカラム一覧を確認します。
2. **[ファイルを選択]** ボタンで新しいCSVファイルを選択します（最大100MB）。
3. 選択したファイルのプレビューと確認事項を確認します。
4. **[上書き]** ボタンをクリックするとアップロードが開始され、進捗は一覧画面で確認します。

アップロードが制限される場合は次のとおりです。

| 状況 | 案内 |
| --- | --- |
| ファイルのカラム数が既存のカラム数より多い | カタログにフィールドを先に追加してから再度アップロードしてください |
| カタログにないカラムが含まれている | カタログにフィールドを先に追加してから再度アップロードしてください |
| ヘッダー設定とファイルの先頭行が一致しない | カラム名行があるファイルでアップロードするか、データソースのヘッダー設定を変更してください |

<a id="datasource.edit.field"></a>
#### フィールドの追加 { #datasource.edit.field }

詳細表示の **[カタログ]** タブで **[フィールドを追加]** ボタンをクリックします。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| フィールド名 | O | 英字または_で始まり、英字、数字、_のみ使用可能 |
| タイプ | O | スキーマでサポートするフィールドタイプから選択 |
| 説明 | X | フィールドの説明 |

- **[行追加]** ボタンで一度に最大 20 個まで追加できます。
- システム予約フィールド名およびすでに存在するフィールド名は使用できません。
- Event API が有効化されているデータソースにはフィールドを追加できません。先に Event API を無効化します。

!!! tip "ヒント"
    コンソールだけでなく、APIでもデータを更新できます。使用方法は [APIガイド](../api-guide/#ingest.api) の「Ingest API」を参照してください。

<a id="pipeline"></a>
## パイプライン { #pipeline }

コンソールパス: **Machine Learning > NHN Cloud Foundry > パイプライン** タブ

パイプラインは、データソースのデータをノードを接続したワークフローで処理し、分析可能なデータセットに変換する機能です。

<a id="pipeline.list"></a>
### パイプラインリスト { #pipeline.list }

パイプラインメニューに移動すると、作成済みのパイプラインの一覧がテーブル形式で表示されます。

| カラム | 説明 |
| --- | --- |
| チェックボックス | パイプラインの選択 |
| 有効化 | 有効/無効のアイコン |
| パイプライン名 | パイプラインを識別する名前 |
| バッチスケジュール | 設定されたスケジュール。未設定の場合はハイフンで表示 |
| 開始日 | スケジュール開始日時 |
| 終了日 | スケジュール終了日時 |
| 最終実行日 | 最後に実行された日時 |
| パイプラインステータス | パイプラインの現在の状態 |
| 手動実行 | 実行ボタン |

- 行をクリックすると、編集画面に移動します。
- 実行中のパイプラインは、手動実行の欄に進行中の表示が現れ、実行できない状態ではボタンが無効化されます。

ツールバーから、パイプラインの作成、修正、削除、有効化、無効化を実行できます。有効化と無効化は **[詳細(⋯)]** メニューにあります。

<a id="pipeline.create"></a>
### パイプライン作成 { #pipeline.create }

1. 一覧画面で **[パイプライン作成]** ボタンをクリックします。
2. 右側の **[設定パネル]** で基本情報を入力します。

    | 項目 | 必須 | 説明 |
    | --- | --- | --- |
    | パイプライン名 | O | パイプラインを識別する名前（最大 30 文字）。韓国語、日本語、英字、数字、スペース、-、_ が使用可能 |
    | パイプライン説明 | X | パイプラインの補足説明（最大 255 文字） |
    | タグ | X | 分類用のタグ（最大 10 個、各 50 文字） |

3. **[作成]** ボタンをクリックすると、パイプラインが下書き状態で作成されます。

!!! danger "注意"
    有効化済みのパイプラインの設定を保存すると、パイプラインが無効化されます。実行ボタンを押してビルドすると、自動的に有効化されます。

<a id="pipeline.editor"></a>
### パイプラインエディター { #pipeline.editor }

パイプラインの作成/編集時に表示されるメイン編集画面です。

- **ヘッダー**: パイプライン名、説明、戻るボタン
- **タブバー（左側）**: 実行/停止、実行履歴、ソースノード追加ボタン
- **タブバー（右側）**: 最終実行日、ステータス/ビルド/有効化バッジ、保存ボタン
- **エディター領域**: ノード-エッジエディター（ドラッグ＆ドロップ、自動整列サポート）
- **サイドパネル**: 設定、スケジュール、コンピューティングリソースパネル

<a id="pipeline.status"></a>
#### パイプラインステータス { #pipeline.status }

ステータスバッジにマウスを乗せると、ツールチップで詳細を確認できます。

| 値 | 説明 |
| --- | --- |
| 下書き | パイプラインが作成され、ノードがない状態 |
| 変更済み | パイプライン設定が変更され、再ビルド（実行）が必要な状態 |
| ビルド中 | パイプラインのビルド中 |
| 待機中 | パイプラインのビルドが完了し、実行準備が整った状態 |
| 実行中 | パイプラインの実行中 |
| 完了 | パイプラインの実行が正常に完了 |
| 失敗 | パイプラインの実行停止またはエラーが発生 |
| 停止済み | ユーザーがパイプラインの実行を停止した状態 |
| 削除中 | パイプラインの削除中 |

<a id="pipeline.node"></a>
### ノード構成 { #pipeline.node }

パイプラインは以下の 5 種類のノードを組み合わせて構成します。

| ノードタイプ | 役割 | 入力 | 出力 | 説明 |
| --- | --- | --- | --- | --- |
| Source（ソースノード） | データ元 | なし | データ | 外部データソースを接続 |
| Transform（変換ノード） | データ加工 | データ | 変換されたデータ | フィルター、派生カラム、集計、ラベルエンコーディングなど |
| Join（結合ノード） | テーブル結合 | 2 つのデータ | 結合されたデータ | Inner/Left/Right/Full Outer/Semi/Anti 結合 |
| Union（ユニオンノード） | テーブル統合 | 2 つのデータ | 統合されたデータ | Full Merge/Intersect Merge/Left-First Merge |
| Dataset（出力ノード） | 出力テーブル | データ | なし | テーブルとして結果を保存（最終ノード） |

接続の制約事項:

- SOURCE ノードには入力を接続できません（ルートノード）。
- DATASET ノードには出力を接続できません（最終ノード）。
- 循環参照（ループ）や自己との接続はできません。

!!! danger "注意"
    保存時にパイプライン構成を検査します。すべてのノードは 1 つのフローで接続されている必要があり（未接続ノード不可）、フローの最終ノードは 1 つの DATASET ノードでなければなりません。

<a id="pipeline.node.source"></a>
#### ソースノードの追加 { #pipeline.node.source }

1. タブバーの **[ソースノードの追加]** ボタンをクリックします。
2. 使用可能なデータソースの一覧がモーダルで表示されます。
3. 目的のソースを選択して追加します。

| 値 | 説明 |
| --- | --- |
| FILE | CSV ファイルデータソース |
| 推薦 | 推薦結果ストレージ |
| データセット | パイプラインのデータセットノードが生成したデータ。他のパイプラインの入力として再利用可能 |

<a id="pipeline.node.transform"></a>
#### 変換ノード(TRANSFORM) { #pipeline.node.transform }

変換作業ごとにノード名（最大 30 文字、韓国語・日本語・英字・数字・空白・`-`・`_`）を指定します。1 つのノードに複数の変換作業を追加すると、順番に接続されたノードが生成されます。

サポートされる変換メソッドは次のとおりです。

| 分類 | メソッド | 説明 |
| --- | --- | --- |
| Row（行単位演算） | Filter | 条件に一致する行のみ抽出 |
| Row（行単位演算） | Explode | 区切り文字列を行に分解 |
| Row（行単位演算） | Derive | ホワイトリスト関数で派生カラムを生成 |
| Aggregation（集計演算） | Aggregate | グループ化および集計 |
| Window（ウィンドウ演算） | Rank Top N | グローバル並べ替え＋上位 N 件 |
| Column（カラム演算） | Label Encode FIT | カテゴリ → 整数マッピングテーブルの生成 |
| Column（カラム演算） | Label Encode Apply | マッピングテーブルによるエンコードの適用 |
| LLM（LLM ベースの演算） | Classify | LLM でテキストをカテゴリに分類 |

<a id="pipeline.node.transform.filter"></a>
##### フィルター(Filter) { #pipeline.node.transform.filter }

| 値 | 説明 | 例 |
| --- | --- | --- |
| = | 等しい | field = 'value' |
| ≠ | 等しくない | field ≠ 'value' |
| > | より大きい | field > 100 |
| ≥ | 以上 | field ≥ 100 |
| &lt; | 未満 | field < 100 |
| ≤ | 以下 | field ≤ 100 |
| LIKE | パターンマッチング | field LIKE '%keyword%' |
| IN | リストに含む | field IN ('a', 'b', 'c') |
| IS NULL | NULL かどうか | field IS NULL |
| IS NOT NULL | NOT NULL かどうか | field IS NOT NULL |

選択したカラムのタイプによって、使用可能な演算子のみが表示されます。

| カラムタイプ | 使用可能な演算子 |
| --- | --- |
| 文字列 | =, ≠, LIKE, IN, IS NULL, IS NOT NULL |
| 数値 | =, ≠, >, ≥, &lt;, ≤, IN, IS NULL, IS NOT NULL |
| 日付/時刻 | =, ≠, >, ≥, &lt;, ≤, IS NULL, IS NOT NULL |
| ブール値 | =, ≠, IS NULL, IS NOT NULL |

比較する値は **[直接入力]**、**[時刻（現在基準）]**、**[別のカラム]** の中から選択します。

論理演算子は `AND`、`OR` をサポートしており、**[グループの追加]** ボタンで条件を最大 3 段階までグループ化できます。

<a id="pipeline.node.transform.explode"></a>
##### 分解(Explode) { #pipeline.node.transform.explode }

区切り文字でつながれた文字列カラムをトークンごとに別の行に分解します。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 分解するカラム | O | 区切り文字でつながれた文字列が含まれるカラム |
| 出力カラム名 | O | 分解された各トークンを格納するカラム名 |
| 区切り文字 | O | デフォルトはカンマ（,） |
| 空白の削除 | X | ON の場合、各トークンの前後の空白を削除 |
| 空トークンの削除 | X | ON の場合、分解後の空文字列トークンを行から除外 |
| 出力タイプ | X | STRING、BIGINT、INT、DOUBLE。未指定の場合は STRING |
| 保持する元カラム | X | 結果に含める元のカラム。選択しない場合はすべての元カラムが保持される |

- 区切り文字は正規表現として解釈されるため、特殊文字はエスケープが必要です（例: `\.`、`\|`）。
- 出力タイプを指定すると、分解された値が該当タイプに変換されます。例えば、数値 ID がカンマで区切られている場合に BIGINT を指定すると、結合キーのタイプを合わせることができます。

<a id="pipeline.node.transform.derive"></a>
##### 派生カラム(Derive) { #pipeline.node.transform.derive }

提供されている関数を使用して、既存のカラムから新しいカラムを作成します。**[派生カラムの追加]** ボタンで定義を複数追加でき、各定義に関数と派生カラム名を指定します。

| 値 | 説明 | 追加設定 |
| --- | --- | --- |
| DATE_BUCKET | 日付バケット（epoch ms → 指定タイムゾーンの日付） | ソースカラム、タイムゾーン（例: Asia/Seoul） |
| JSON_ARRAY_LENGTH | JSON 配列の要素数 | ソースカラム、要素スキーマ |
| CONCAT | 複数のカラムを区切り文字で結合 | ソースカラム 2 つ以上、区切り文字、デフォルト値（任意） |
| RATIO | 分子/分母の比率計算 | 分子、分母、倍数（任意、デフォルト 1）、丸め桁数（任意） |
| COALESCE | 複数カラムのうち最初の non-null 値 | ソースカラム 2 つ以上 |

- 定義は上から下へ順番に適用され、前に作成した派生カラムを後の定義のソースカラムとして参照できます。
- 派生カラム名には英字、数字、アンダースコア（_）、韓国語文字を使用できます。

<a id="pipeline.node.transform.aggregate"></a>
##### 集計(Aggregate) { #pipeline.node.transform.aggregate }

1. **[グループ化基準の選択]**（任意）: 同じ値を持つ行をグループにまとめます。選択しない場合は、全データを 1 つのグループとして集計します。
2. **[集計関数の定義]**（必須）: 集計するカラム、集計関数、結果カラム名を指定します。複数追加できます。

| 値 | 説明 |
| --- | --- |
| COUNT | 件数 |
| COUNT_DISTINCT | 一意の値の件数 |
| SUM | 合計 |
| AVG | 平均 |
| MIN | 最小値 |
| MAX | 最大値 |
| FIRST | 最初の値 |
| LAST | 最後の値 |
| STDDEV | 標準偏差 |
| VARIANCE | 分散 |
| ARRAY_AGG | グループの値を配列として収集 |

- COUNT は集計するカラムとして `* (全体)` を選択できます。
- FIRST と LAST は並べ替え基準カラムと方向（昇順/降順）を指定し、並べ替え後の最初または最後の値を取得します。

ARRAY_AGG を選択すると、次の設定が追加されます。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 配列要素の種類 | O | 単一カラムまたは JSON オブジェクト |
| 収集するカラム | O* | 配列要素の種類が単一カラムの場合に必須。配列に格納するカラム |
| JSON オブジェクトフィールドの定義 | O* | 配列要素の種類が JSON オブジェクトの場合に必須。キー名とカラムの組み合わせ |
| 配列要素の並べ替え | X | 並べ替え基準カラムと方向。未指定の場合は並べ替えなし |
| 出力形式 | O | STRUCT_ARRAY、JSON_STRING_ARRAY、JSON_STRING |
| 重複除去（DISTINCT） | X | 並べ替えを設定すると無視される |

<a id="pipeline.node.transform.rank"></a>
##### ランキング(Rank Top N) { #pipeline.node.transform.rank }

全データを指定した基準で並べ替え、上位 N 行のみを残します。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 並べ替え基準 | O | 並べ替えるカラムと方向（DESC / ASC） |
| 上位 N 件 | O | 並べ替え後に結果に含める行数 |
| 結果順位カラム名 | O | 結果に追加される 1〜N の順位カラムの名前 |

- **[並べ替え基準の追加]** ボタンで基準を複数指定すると、第 1 ソート、第 2 ソートの順に適用され、ドラッグして順序を変更できます。
- 参照時に順位カラムで並べ替えると、元の順序を維持できます。

<a id="pipeline.node.transform.label.encode"></a>
##### ラベルエンコーディング(Label Encode FIT / Label Encode Apply) { #pipeline.node.transform.label.encode }

カテゴリ型データを数値（整数）に変換する機能です。**FIT（学習）** と **APPLY（適用）** の 2 段階で構成され、必ず FIT ノードを先に作成してから APPLY ノードで参照する必要があります。

FIT ノード設定:

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| エンコードするカラムの選択 | O | ラベルエンコーディングを適用するカラム（1 つ以上） |
| 開始インデックス | X | エンコードの開始値（デフォルト: 0） |

APPLY ノード設定:

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| マッピングテーブルノードの選択 | O | FIT ノードを選択 |
| 元カラムの上書き | X | チェック時、元カラムをエンコードされた値に置き換える（デフォルト: 無効） |
| 結果カラムのサフィックス | X | 元カラムを保持する場合に新カラム名に付くサフィックス（デフォルト: _encoded） |

!!! tip "ヒント"
    マッピングテーブルにない値（OOV、Out-of-Vocabulary）は `-1` としてエンコードされます。

!!! danger "注意"
    FIT を再実行すると、データの変更によりエンコード番号が変わる場合があります。既存モデルとの互換性に注意してください。

<a id="pipeline.node.transform.classify"></a>
##### 分類(Classify) { #pipeline.node.transform.classify }

LLM を使用してテキストカラムの値をカテゴリに分類します。分類の基準となるカテゴリリストは、別のデータソースノードとして準備する必要があります。

パイプラインが実行されると、入力データの行をバッチサイズ分まとめて、バッチごとに 1 回 LLM を呼び出します。各呼び出しのプロンプトにはカテゴリリストと該当バッチの行が含まれ、LLM が行ごとに適切なカテゴリ ID を返すと、その結果が分類結果カラムとして保存されます。

**LLM 設定**

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| LLM プロバイダー | O | Claude |
| モデル名 | O | claude-haiku-4-5、claude-sonnet-4-6 |
| API キー | O | 使用する LLM の API キー。このパイプラインにのみ保存され、保存後は再表示されません |
| バッチサイズ | X | 1 回の LLM 呼び出しに含める行数（1〜30） |
| 最大リトライ回数 | X | 呼び出し失敗時のリトライ回数（0〜10） |
| タイムアウト（秒） | X | 呼び出し待機時間（1〜600） |
| 増分分類 | X | 変更された行のみ LLM で分類し、変更のない行は前回の実行結果を再利用 |

**入力カラム**

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 入力カラム | O | LLM に渡すカラム。複数選択可 |
| カラム結合区切り文字 | X | 複数の入力カラムの値を連結する際に使用する区切り文字 |
| ユーザープロンプトテンプレート | X | 行ごとの入力値の構成テンプレート。{{ カラム名 }} パターンが該当カラムの値に置換され、設定すると結合区切り文字より優先して適用されます。入力カラムで選択したカラムのみ参照可能 |

**軸（カテゴリテーブル）**

分類の基準となるカテゴリリストを軸として登録します。軸は最大 5 つまで追加できます。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| カテゴリノード | O | カテゴリリストが含まれるデータソースノード |
| カテゴリ ID カラム | O | カテゴリを識別するカラム |
| カテゴリ名カラム | O | カテゴリ名カラム |
| カテゴリメタデータカラム | X | カテゴリの説明、判定基準などの追加カラム。プロンプトとともに渡され、分類精度を向上 |
| 分類結果カラム（配列） | O | 分類結果を格納する配列カラム名 |
| 代表カラム | X | 最も代表的な分類 ID を保存するカラム名。空白にすると生成しない |

**出力**

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 出力タイプ | O | SINGLE_LABEL、MULTI_LABEL |
| 分類根拠カラム | X | LLM が提供した分類根拠を保存するカラム名。空白にすると根拠を要求しないためトークンコストを削減 |
| コンテンツ根拠カラム | X | 入力コンテンツから生成した短い説明を保存するカラム名 |
| コンテンツ根拠の最大長 | X | コンテンツ根拠カラムの最大文字数（1〜200） |

**システムプロンプト**（任意）: LLM に役割と分類基準を指示します。設定しない場合はデフォルトのプロンプトが使用されます。

!!! danger "注意"
    処理する行数が多いほど、LLM の呼び出し回数とトークン使用量が比例して増加し、コストが増加します。大規模データは先に小規模で検証してコストと品質を確認してから適用してください。

<a id="pipeline.node.join"></a>
#### 結合ノード(JOIN) { #pipeline.node.join }

2 つのデータストリームを結合します。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| ノード名 | O | 結合結果ノードの名前（最大 30 文字） |
| 結合タイプ | O | Inner Join / Left Join / Right Join / Full Outer Join / Semi Join（EXISTS）/ Anti Join（NOT EXISTS）から選択 |
| 結合テーブル | O | 左/右テーブルノードの選択 |
| 結合条件 | O | 左フィールド = 右フィールドのマッピング（複数条件可） |
| カラムの選択 | X | 結果に含めるカラムの選択、プレフィックスの設定が可能 |

!!! tip "ヒント"
    両テーブルに同じ名前のカラムがある場合、右テーブルに **[プレフィックス]** を設定してカラム名の競合を防ぐことができます。

<a id="pipeline.node.union"></a>
#### ユニオンノード(UNION) { #pipeline.node.union }

2 つのデータストリームを垂直に統合します。

| 値 | 説明 |
| --- | --- |
| Full Merge | 両側の全カラムを含む。ない側は NULL |
| Intersect Merge | 両側の共通カラムのみを含む |
| Left-First Merge | 左テーブルのスキーマ基準。右側にないカラムは NULL |

<a id="pipeline.node.dataset"></a>
#### データセットノード(DATASET) { #pipeline.node.dataset }

パイプラインの最終出力ノードで、処理されたデータをテーブルとして保存します。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| データセット名 | O | テーブル名として使用（最大 30 文字）。英小文字、数字、_ のみ使用可能で、数字から始めることはできません |
| 説明 | X | データセットの説明（最大 500 文字） |
| カラムの選択 | O | データセットに含めるカラムの選択（最低 1 つ以上） |

データセット名はデータソース名とテーブル名として共用されるため、すでに使用中のデータソース名・テーブル名と重複することはできません。重複チェックはパイプラインの保存時と実行時にそれぞれ行われます。

!!! danger "注意"
    実行済みのデータセットは修正できません。削除して再作成してください（データソースは保持されます）。

<a id="pipeline.schedule"></a>
### スケジュール設定 { #pipeline.schedule }

右側のサイドパネルのスケジュールアイコンをクリックして、バッチスケジュールを設定します。

| 周期タイプ | 設定項目 | 例 |
| --- | --- | --- |
| 毎分 | 間隔（5/10/15/20/30分） | 10分ごとに実行 |
| 毎時間 | 間隔（1/2/3/4/6/8/12時間）、分 | 2時間ごとに30分に実行 |
| 毎日 | 時、分 | 毎日09時30分に実行 |
| 毎週 | 曜日（複数選択可）、時、分 | 毎週月・水・金 09時00分に実行 |
| 毎月 | 日、時、分 | 毎月1日09時30分に実行 |

日付範囲設定：

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 開始日設定 | X | チェック時、設定した日付以降からスケジュールを開始します。未設定の場合、保存時点から開始します。 |
| 終了日設定 | X | チェック時、設定した日付以降はスケジュールが実行されません。 |

スケジュールを保存し、初回実行後から設定した周期に従って実行されます。

<a id="pipeline.resource"></a>
### コンピューティングリソース設定 { #pipeline.resource }

右側のサイドパネルのコンピューティングリソースアイコンをクリックして、パイプライン実行時に使用するコンピューティングリソースを設定します。

| タイプ | Driver | Executor |
| --- | --- | --- |
| Small（デフォルト） | 1 CPU、1GB メモリ | 1 CPU、1GB メモリ × 1個 |
| Medium | 1 CPU、2GB メモリ | 1 CPU、2GB メモリ × 2個 |
| Large | 2 CPU、4GB メモリ | 2 CPU、4GB メモリ × 4個 |
| XLarge | 4 CPU、8GB メモリ | 4 CPU、8GB メモリ × 8個 |

!!! danger "注意"
    リソース設定を変更すると、再ビルド後に適用されます。パイプラインの作成前は、リソースを変更することはできません。

<a id="pipeline.run"></a>
### パイプラインの実行 { #pipeline.run }

初回または設定変更後の初回実行時には、ビルドと実行が同時に行われます。

1. タブバーで **[実行]** ボタンをクリックします。
2. 確認モーダルで **[実行]** ボタンをクリックします。

実行は最後に保存した構成で行われます。未保存の変更がある場合は、保存前の構成で実行される旨を通知する確認モーダルが最初に表示されます。変更内容を含めて実行するには、先に保存してください。
すでにビルド済みのパイプラインは、ビルド処理なしに直接実行されます。
実行中に **[停止]** ボタンをクリックすると、終了状態に移行します。
ビルドが完了すると、スケジュールが設定されているパイプラインは自動的に有効化されます。

<a id="pipeline.run.history"></a>
#### 実行履歴の確認 { #pipeline.run.history }

タブバーで **[実行履歴]** ボタンをクリックすると、実行 ID、開始/終了日時、所要時間、実行状態、ノード別の実行詳細を確認できます。

<a id="pipeline.activation"></a>
### 有効化/無効化 { #pipeline.activation }

- **[有効化]**: 一覧からパイプラインを1つ選択し、その他(⋯)メニューで **[パイプラインの有効化]** をクリックします。設定されたスケジュールに従い、自動的に実行されます。
- **[無効化]**: 一覧からパイプラインを1つ選択し、その他(⋯)メニューで **[パイプラインの無効化]** をクリックします。スケジュールが設定されていても、自動実行されません。

<a id="pipeline.delete"></a>
### パイプラインの削除 { #pipeline.delete }

1. 一覧から削除するパイプラインを選択します（複数選択可能）。
2. **[削除]** ボタンをクリックします。
3. 確認モーダルで **[削除]** をクリックします。

<a id="query"></a>
## 分析 - クエリ { #query }

コンソールパス: **Machine Learning > NHN Cloud Foundry > 分析** タブ > **クエリ** タブ

SQL を使用して、データソースのデータを照会・分析します。

<a id="query.run"></a>
### クエリ実行 { #query.run }

1. **[データソース]** を選択します。
2. **[クエリ選択]** で保存済みクエリを読み込みます。選択したデータソースで保存したクエリがある場合は一覧に表示されます。読み込まずに直接記述することもできます。
3. クエリ入力欄に SQL を記述します。
4. **[行制限]** を選択します（10、100、1,000、10,000、100,000 / デフォルト 1,000）。
5. **[クエリ実行]** ボタンをクリックします。

- 実行結果はデータグリッド形式で表示されます。カラム構造はクエリ結果に応じて動的に生成され、ページネーションをサポートします。
- データソースを選択すると、クエリ入力欄の右側にスキーマパネルが表示され、フィールド名とデータ型を確認できます。フィールド名で検索できます。
- クエリ入力欄で **Ctrl+Enter**（macOS は **⌘+Enter**）を押すとクエリが実行されます。
- クエリの実行に失敗した場合、失敗の原因が結果エリアに表示されます。
- **[初期化]** ボタンをクリックすると、記述したクエリがクリアされます。
- **[クエリ結果のダウンロード]** ボタンをクリックすると、結果が CSV ファイルとして保存されます（ファイル名: `クエリ名_日付_時刻.csv`）。
- FROM 句には、データソース一覧に表示されたテーブル名をそのまま使用します（`SELECT * FROM {テーブル名}`）。
- 単一の SELECT 文のみ実行できます。それ以外の文は拒否されます。

<a id="query.save"></a>
### 保存済みクエリの変更 { #query.save }

**[クエリ選択]** で読み込んだクエリの内容を変更した後、**[クエリ保存]** ボタンをクリックすると変更が保存されます。読み込んだクエリがない場合や内容を変更していない場合は、ボタンが無効になります。

<a id="query.list"></a>
### クエリ一覧 { #query.list }

クエリは実行のたびに実行履歴として自動的に記録されます。繰り返し使用するクエリはクエリ一覧に直接登録しておくことができます。**[クエリ一覧]** ボタンをクリックすると、保存済みクエリを管理するモーダルが開きます。

| カラム | 説明 |
| --- | --- |
| クエリ名 | 保存時に指定した名前 |
| クエリ構文 | 保存済みの SQL |
| 保存日時 | クエリを最後に保存した日時 |
| 詳細 | クエリの全内容を照会 |

- クエリ名で検索できます。
- **[追加]** ボタンで新しいクエリを登録し、クエリを選択した後、**[修正]** または **[削除]** ボタンで編集または削除します。削除したクエリは復元できません。

追加と修正時に入力する項目は次のとおりです。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| クエリ名 | O | クエリを識別するための名前 |
| データソース | O | クエリを実行するデータソース |
| 構文 | O | 実行する SQL |

<a id="query.history"></a>
### クエリ実行履歴 { #query.history }

これまでに実行したクエリを照会する画面です。**[時間]** の範囲と **[クエリ内容]** で検索し、**[初期化]** ボタンで検索条件をクリアします。

| カラム | 説明 |
| --- | --- |
| クエリ名 | 実行したクエリの名前 |
| クエリ実行日 | クエリを実行した日時 |
| データソース名 | クエリを実行したデータソース |
| クエリ/結果 | 実行した SQL と結果 |

一覧の項目をクリックするとクエリの詳細が表示されます。**[クエリを使用]** をクリックすると、該当クエリを実行画面に読み込みます。

<a id="chart"></a>
## 分析 - チャート { #chart }

コンソールパス: **Machine Learning > NHN Cloud Foundry > 分析** タブ > **チャート** タブ

<a id="chart.list"></a>
### チャート一覧 { #chart.list }

| カラム | 説明 |
| --- | --- |
| チャート名 | チャートを識別する名前 |
| チャートタイプ | チャートの用途区分 |
| ビジュアライゼーションタイプ | チャートビジュアライゼーションタイプ |
| データソース名 | チャートが使用するデータソース名 |
| Group Value | 値ごとに分けて表示するよう指定したカラム |
| 作成日 | チャートが作成された日時 |
| 変更日 | チャートが最後に更新された日時 |

- 上部の検索機能でチャート名によるフィルタリングができます。
- データソースが削除されて名前が見つからない場合は、データソース ID が表示されます。

<a id="chart.create"></a>
### チャートの作成 { #chart.create }

**[チャートの作成]** ボタンをクリックすると、チャートエディター画面に移動します。エディターで次の項目を順番に設定します。

<a id="chart.create.basic"></a>
#### 基本設定 { #chart.create.basic }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| チャート名 | O | チャートを識別するための名前（最大 30 文字） |
| チャートタイプ | O | チャートの用途区分（現在は基本のみ提供） |
| チャート可視化タイプ | O | Line Chart、Bar Chart、Pie Chart、Scatter Chart |

<a id="chart.create.datasource"></a>
#### データソース設定 { #chart.create.datasource }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| データソースタイプ | O | FILE、DATASET、RECOMMENDATION SINK |
| データソース名 | O | 使用するデータソースを選択 |

<a id="chart.create.query"></a>
#### クエリ設定 { #chart.create.query }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| X軸 | O* | 横軸に使用するフィールド。Pie チャートでは表示されません |
| 集計間隔 | O* | 5分、15分、30分、45分、1時間、2時間、6時間、12時間、1日、3日、7日。Line・Bar チャートでのみ表示 |
| 基準時間 | O | データの時間範囲を指定 |
| カラム | O | 集計対象のカラムと集計関数を選択。1つ以上必要 |
| グループキー | O* | 値ごとに分けて表示するカラム。Pie チャートは1つ以上必要。その他は任意 |
| フィルター | X | データフィルタリング条件を設定 |
| ソート | X | ソートするカラムと方向。Pie・Bar チャートでのみ表示 |
| 行制限 | O | 取得する最大行数 |

チャート可視化タイプごとに必要な設定は次のとおりです。

| チャート可視化タイプ | 必須設定 |
| --- | --- |
| Line、Bar | X軸（時間軸）、集計間隔、カラム1つ以上 |
| Pie | 基準時間カラム、カラム1つ以上、グループキー1つ以上 |
| Scatter | カラム1つ以上 |

Bar チャートにソートを指定すると、時間軸の代わりに上位 N 件のカテゴリで表示されます。

<a id="chart.create.preview"></a>
#### チャートプレビュー { #chart.create.preview }

設定完了後、**[UPDATE CHART]** ボタンをクリックしてプレビューを確認します。

- チャートが正常に表示されるか確認します。
- 下部のテーブルビューでデータを確認できます。テーブルビューの表示/非表示を切り替えることもできます。
- 照会に失敗した場合、チャート領域に **[チャート照会失敗]** パネルが表示され、クエリエンジンが返したエラーの内容をそのまま確認できます。エラーの内容を参考にクエリ設定を修正し、再度照会します。

<a id="chart.create.save"></a>
#### チャートの保存 { #chart.create.save }

- プレビューを確認後、ヘッダーの **[作成]** ボタンをクリックします。
- 入力内容の検証が完了すると、作成ボタンが有効になります。

<a id="chart.edit"></a>
### チャートの編集 { #chart.edit }

チャート一覧でチャートをクリックして、編集画面に移動します。

- 基本設定とクエリ設定を変更できます。
- データソース設定は現在使用中のデータソースを表示しますが、変更できないようにロックされた状態で表示されます。別のデータソースを使用するには、チャートを新たに作成します。
- **UPDATE CHART** でプレビューを確認後、**[保存]** で変更内容を保存します。

<a id="chart.delete"></a>
### チャートの削除 { #chart.delete }

1. チャート一覧で削除するチャートをチェックボックスで選択します。
2. **[削除]** ボタンをクリックします。
3. 確認モーダルで **[削除]** をクリックします。

!!! danger "注意"
    ダッシュボードで使用中のチャートを削除すると、該当するダッシュボードからも削除されます。

<a id="dashboard"></a>
## 分析 - ダッシュボード { #dashboard }

コンソールパス: **Machine Learning > NHN Cloud Foundry > 分析** タブ > **ダッシュボード** タブ

<a id="dashboard.list"></a>
### ダッシュボード一覧 { #dashboard.list }

- 検索機能でダッシュボード名のフィルタリングができます。
- 1ページあたりの表示件数を調整できます。

<a id="dashboard.create"></a>
### ダッシュボードの作成 { #dashboard.create }

**[ダッシュボードの作成]** ボタンをクリックすると、ダッシュボードエディター画面に移動します。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| ダッシュボード名 | O | ダッシュボードを識別するための名前 |
| ダッシュボードの説明 | X | ダッシュボードの説明 |

編集パネルは2つのタブで構成されます。

| タブ | 説明 |
| --- | --- |
| CHARTS | ダッシュボードに追加できるチャートの一覧。チャート名で検索可能 |
| LAYOUT ELEMENTS | キャンバスに追加するレイアウト要素 |

<a id="dashboard.create.chart"></a>
#### チャートの追加 { #dashboard.create.chart }

1. 追加するチャートカードをクリックまたはドラッグします。
2. ダッシュボードのキャンバスにチャートを配置します。

<a id="dashboard.create.tabgroup"></a>
#### タブグループの追加 { #dashboard.create.tabgroup }

複数のチャートをタブにまとめて、同じ場所で切り替えながら表示できます。編集パネルの **[LAYOUT ELEMENTS]** タブで **[Tab]** をクリックすると、キャンバスにタブグループが追加されます。

- タブグループの **[タブの追加]** でタブを増やします。タブが2つ以上ある場合、各タブに削除アイコンが表示されます。タブが1つだけ残った場合は削除できません。
- タブ名はタブをダブルクリックして変更します。
- 編集パネルのチャートをタブ領域にドラッグ＆ドロップすると、そのタブに配置されます。すでにキャンバスにあるチャートは、チャートタイトル左のハンドルをドラッグしてタブに移動できます。タブ内のチャートも同じ方法でキャンバスに戻せます。
- タブグループはチャートと同様に、右上のゴミ箱アイコンをクリックしてダッシュボードから削除します。

<a id="dashboard.create.layout"></a>
#### レイアウトの調整 { #dashboard.create.layout }

キャンバスに配置したチャートはドラッグして位置を移動し、角をドラッグしてサイズを調整します。チャート右上のゴミ箱アイコンをクリックすると、ダッシュボードから削除されます。

<a id="dashboard.create.save"></a>
#### ダッシュボードの保存 { #dashboard.create.save }

設定完了後、ヘッダーの **[保存]** ボタンをクリックします。

<a id="dashboard.view"></a>
### ダッシュボードの閲覧 { #dashboard.view }

ダッシュボード一覧でダッシュボードをクリックして、詳細画面に移動します。

<a id="dashboard.edit"></a>
### ダッシュボードの編集 { #dashboard.edit }

ダッシュボード詳細画面で **[編集モードトグル]** をクリックして、編集モードに切り替えます。

編集モードでは、次の操作が可能です。

- チャートの追加・削除・位置の変更
- ダッシュボード名の変更

ダッシュボードに配置されたチャートを修正するには、編集モードをオフにします。編集モードがオフの場合、各チャートに詳細メニューが表示され、**[チャートの修正]** を選択するとチャート編集ページに移動します。タブグループ内のチャートも同じ方法で移動でき、チャート編集後に保存するとダッシュボードに変更内容が反映されます。

<a id="dashboard.delete"></a>
### ダッシュボードの削除 { #dashboard.delete }

1. ダッシュボード一覧で削除するダッシュボードを選択します。
2. **[削除]** ボタンをクリックします。
3. 確認モーダルで **[確認]** をクリックします。

<a id="app"></a>
## アプリ { #app }

コンソールパス: **Machine Learning > NHN Cloud Foundry > アプリ** タブ

AI モデルをデータに接続するアプリを作成および管理します。アプリのタイプは**推薦システム**と**単変量異常検出**の2種類を提供します。

| アプリタイプ | 説明 |
| --- | --- |
| 推薦システム | ユーザーの行動パターンを分析し、パーソナライズされた推薦を提供します。 |
| 単変量異常検出 | メトリクスを1つずつ学習し、正常範囲を外れた値を検出します。 |

<a id="app.list"></a>
### アプリ一覧 { #app.list }

| カラム | 説明 |
| --- | --- |
| アプリタイプ | アプリのタイプ |
| アプリID | アプリの固有識別子 |
| アプリ名 | アプリの識別名 |
| アプリの説明 | アプリの説明 |
| ステータス | アプリのステータス |
| 作成日時 | アプリの作成日時 |

<a id="app.list.status"></a>
#### アプリのステータス { #app.list.status }

アプリのステータスバッジにマウスを重ねると、ツールチップで詳細説明を確認できます。

| 値 | 説明 |
| --- | --- |
| 初期化中 | アプリの初期化が進行中 |
| 学習中 | AIモデルの学習が進行中 |
| デプロイ中 | アプリのデプロイが進行中 |
| アクティベーション中 | アプリのアクティベーションが進行中 |
| アクティブ | アプリのアクティブ状態 |
| 削除中 | アプリの削除が進行中 |
| 失敗 | アプリの処理中にエラーが発生 |
| 不明 | ステータス情報を確認不可 |

- アプリ作成後、自動的に学習およびデプロイが実行されます。
- 各ステップでエラーが発生した場合、失敗ステータスに移行します。

!!! tip "ヒント"
    以下は推薦システムアプリを基準にした説明です。アプリ作成直後に行われる学習・デプロイはアプリを準備する過程であり、この時点では推薦モデルがまだ学習されていない状態です。推薦 API を呼び出すとレスポンスは返されますが、学習済みモデルの推薦結果ではありません。
    最初の学習はバッチスケジュールに指定した時刻に実行され（ステータス: 学習中 → デプロイ中 → 有効化中 → アクティブ）、学習済みモデルがデプロイされた後から有効な推薦結果を照会できます。

<a id="app.create"></a>
### アプリの作成 { #app.create }

**[アプリ作成]** ボタンをクリックすると、アプリ作成画面に切り替わります。アプリの作成は3ステップで進みます。

| ステップ | 説明 |
| --- | --- |
| 基本設定 | アプリ名、説明、タイプの選択 |
| 詳細設定 | アプリタイプに応じたモデル、データ接続、リソースの設定 |
| 最終確認 | 入力内容の確認および作成 |

アプリ作成画面の下部には、ステップに関係なく**リソース確認**の結果が表示されます。アプリのタイプに関係なく表示され、作成可能かどうかをアプリを作成する前に確認できます。

| 案内 | 説明 |
| --- | --- |
| アプリを作成できるリソースがあります。 | 作成可能 |
| アプリを作成できますが、残りのリソースが不足しているため、再学習結果の反映が遅延する場合があります。 | 作成は可能ですが余裕不足 |
| 割り当てられたリソースが不足しているため、アプリをさらに作成することが困難です。 | 作成不可。次のステップに進むことができません |
| 割り当てられたリソースが不足しているため、学習を実行できません。 | アプリは作成できますが、学習を実行する余裕がありません。 |
| 使用可能な GPU がありません。 | GPU を使用するアプリをこれ以上作成できません。既存の GPU アプリを削除するか、CPU を選択してください。 |
| 現在のサービス環境では GPU を使用できません。 | クラスターに GPU ノードがないため、デバイスで GPU を選択できません |
| リソースの状態を確認できませんでした。 | 判定失敗。リソースが不足している場合、アプリが正常に起動しない可能性があります |

- リソースが不足している場合は、既存のアプリを整理するか、1:1お問い合わせでリソースの増設を申請します。
- アプリを削除した直後は、リソースが回収されるまで約5分かかる場合があります。
- 判定結果は保存されないため、アプリ作成画面に移動するたびに再度照会します。

<a id="app.create.basic"></a>
#### 基本設定 { #app.create.basic }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| アプリ名 | O | アプリを識別するための名前（最大255文字）。ハングル、日本語、英字、数字、スペース、-、_ が使用可能 |
| アプリの説明 | O | アプリの説明 |
| アプリタイプ | O | **推薦システム** または **単変量異常検知** を選択。タイプを選択すると、下にタイプの説明が表示されます |

<a id="app.create.detail"></a>
#### レコメンデーションシステムの詳細設定 { #app.create.detail }

**[モデル追加]** ボタンでモデルカードを追加します。1つのアプリに複数のモデルを組み合わせて構成でき、モデルカードごとに以下のセクションを設定します。

<a id="app.create.detail.model"></a>
##### 基本モデル設定 { #app.create.detail.model }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| モデル名 | O | 使用する推薦モデルの選択 |
| Longtail モード | X | 有効にすると、人気度の低いアイテムも推薦に含まれます |

選択可能なモデルは次のとおりです。

| 名前 | 説明 |
| --- | --- |
| Cold User | 新規ユーザーなどまだ活動履歴が少ないユーザーに、アイテム自体の属性を基に推薦 |
| Warm User(Transformer) | ユーザーが最近インタラクションしたアイテムの順序に基づき、次のアイテムを推薦 |

同じモデルでも Longtail モードが異なれば、別のモデルとして追加できます。

<a id="app.create.detail.resources"></a>
##### サービングリソース設定 { #app.create.detail.resources }

モデルごとのサービングコンテナに割り当てるリソースを指定します。入力しない場合はデフォルト値が適用されます。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| CPU request / CPU limit | X | Core 単位。デフォルト値 2 / 4 |
| メモリ request / メモリ limit | X | Gi 単位。デフォルト値 4 / 4 |

`request` は `limit` を超えることはできません。

<a id="app.create.detail.schedule"></a>
##### バッチスケジュール設定 { #app.create.detail.schedule }

モデルを再学習するバッチの実行周期を設定します。デフォルトで有効になっており、トグルでオフにできます。
モデルの最初の学習もこのスケジュールに設定した時刻に実行され、最初の学習が完了する前の推薦レスポンスは学習済みモデルの結果ではありません。

| 周期 | 設定項目 |
| --- | --- |
| 毎日 | 時、分 |
| 毎週 | 曜日、時、分 |
| 毎時 | 時間間隔、分 |

設定した時刻は、アクセスしているブラウザのタイムゾーンを基準に適用されます。

<a id="app.create.detail.connection"></a>
##### データ接続設定 { #app.create.detail.connection }

選択したモデルに応じて、必要なデータソースを接続します。

一般推薦モデル:

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| ユーザーデータソース | O | ユーザー情報のデータソース |
| ユーザーIDカラム | O | ユーザー識別カラム |
| ユーザーFeatureカラム | X | 追加のユーザー特性カラム（複数選択可能） |
| アイテムデータソース | O | アイテム情報のデータソース |
| アイテムIDカラム | O | アイテム識別カラム |
| アイテムFeatureカラム | X | 追加のアイテム特性カラム（複数選択可能） |
| 履歴データソース | O | ユーザー・アイテムインタラクションのデータソース |
| 履歴ユーザーIDカラム | O | 履歴でユーザーを識別するカラム |
| 履歴アイテムIDカラム | O | 履歴でアイテムを識別するカラム |
| 時間カラム | X | インタラクション時間のカラム |
| 履歴Featureカラム | X | 追加のインタラクション特性カラム |

タグ埋め込みモデル:

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| タグデータソース | O | タグ情報のデータソース |
| 属性カラム | O | タグ値のカラム |
| アイテムIDカラム | O | アイテム識別カラム |
| 時間カラム | X | 時間カラム |

<a id="app.create.detail.extra"></a>
##### 追加設定（Skills） { #app.create.detail.extra }

推薦理由（reason）の構成に使用するスキル・カテゴリデータを接続するオプション設定です。**[フィールド追加]** ボタンで追加します。

| 項目 | 説明 |
| --- | --- |
| スキルデータソース | スキル情報テーブル。スキルIDカラムとスキルカラムを指定 |
| 基本スキルカテゴリ / 共通スキルカテゴリ | スキル分類テーブル。スキルIDカラムとスキルラベルカラムを指定 |
| ユーザーグループデータソース | 推薦対象ユーザーをまとめる単位（例: 部署、学年、興味グループ）。ユーザーグループIDカラムとスキルカラムを指定 |
| ユーザー別関心スキルデータソース | ユーザー別の関心スキルテーブル。推薦APIに関心スキルが渡されなかった場合、このテーブルを参照して関心に基づく推薦理由を生成 |
| ユーザー属性マッピング | 推薦APIの userAttributes で各項目を渡すキー名を指定 |
| 推薦理由テンプレートデータソース | 推薦理由の文言テンプレートテーブル。未選択の場合、推薦結果に理由は含まれません |
| コールドスタートデータソース | このテーブルに含まれるユーザーIDのみコールドスターターと判定。データソースとユーザーIDカラムを合わせて選択する必要があります |

<a id="app.create.detail.univariate"></a>
#### 単変量異常検知の詳細設定 { #app.create.detail.univariate }

単変量異常検知アプリは、メトリクスを1つずつ個別に学習し、正常範囲を逸脱した値を検知します。詳細設定は、データソース、モデルリソース、再学習、検知オプション、結果転送の順で構成されます。

<a id="app.create.detail.univariate.source"></a>
##### データソース { #app.create.detail.univariate.source }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 指標データソース | O | 検知対象のメトリクスが入力されるデータソース。タイプが Prometheus API のデータソースのみ選択可能 |

- タイプが Prometheus API のデータソースを先に作成する必要があります。作成後にリストに表示されます。
- 選択したデータソースに指定したシリーズ識別ラベルとグループラベルが、時系列とグループを分ける基準になります。
- 1つの指標データソースに対して、単変量異常検知アプリは1つのみ作成できます。他の単変量異常検知アプリがすでに使用中のデータソースはリストに表示されません。

<a id="app.create.detail.univariate.resource"></a>
##### モデルリソース { #app.create.detail.univariate.resource }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| CPU リミット | O | 推論サーバーに割り当てる CPU 上限。Kubernetes 表記を使用。例: 2、500m。最小 2 vCPU、デフォルト 2 |
| メモリリミット | O | 推論サーバーに割り当てるメモリ上限。Kubernetes 表記を使用。例: 1Gi、512Mi。最小 1Gi、デフォルト 1Gi |

モデルリソースは、アプリを作成した後は変更できません。

<a id="app.create.detail.univariate.retrain"></a>
##### 再学習 { #app.create.detail.univariate.retrain }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 再学習周期 | X | トグルで使用するかどうかを指定。デフォルトはオフ。オンにすると下記の周期を指定 |

| 周期 | 設定項目 |
| --- | --- |
| 毎日 | 時と分。時は 0〜23、分は 10 分単位。デフォルトは 03 時 00 分 |
| 毎週 | 曜日、時、分 |
| 時間間隔 | 間隔と分。間隔は 1、2、3、4、6、8、12 時間から選択 |

- 設定した時刻は、接続しているブラウザのタイムゾーンを基準に適用されます。
- 周期を指定しない場合は、デフォルトの周期で再学習します。
- 学習はリソースを多く消費するため、トラフィックが少ない時間帯を指定することをお勧めします。

<a id="app.create.detail.univariate.option"></a>
##### 検知オプション { #app.create.detail.univariate.option }

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 転送モード | O | 異常スコアとしきい値をいつから出力するかを選択 |
| スコアスケール | X | **[0〜100(%) スケールで出力]** トグル。デフォルトはオフ |
| デバイス | O | 推論と学習に使用する演算デバイス。CPU または GPU から選択。デフォルトは CPU |

転送モードは次の中から選択します。

| 値 | 説明 |
| --- | --- |
| 正確モード | デフォルト。メトリクスの準備が完了した後から、信頼できる値のみ転送 |
| 即時モード | 有効化直後から転送。準備完了前の値は参考値 |

- アプリはメトリクスを 1 分単位でまとめ、時系列ごとに判定します。アプリに接続したデータソースには、同じ時系列を 1 分以下の間隔で途切れなく送信する必要があります。それより長い間隔で送信すると空白区間が生じ、正確モードで準備が完了しない場合があります。データソースへのメトリクスの積載自体は、送信周期とは無関係です。
- 新たに入力されたメトリクスは、判定に使用するデータが蓄積され、しきい値がそのメトリクスの分布で補正されるまで数時間かかります。
- 転送が数分以上途切れると、蓄積された区間が失われ、再び準備状態に戻ります。正確モードでは、再度蓄積が完了するまで結果は出力されません。
- スコアスケールをオンにすると、スコアを 0〜1 に圧縮してから 100 を掛けて 0〜100 で出力し、しきい値も同じスケールで計算します。パーセント軸を使用するダッシュボードとスケールを合わせる際に使用します。オフにすると元の値をそのまま出力します。
- 推論と学習には同じデバイスが適用されます。デフォルトの CPU でも学習と推論が動作します。GPU はサービス環境のリソース状況によって選択できない場合があります。

!!! danger "注意"
    スコアスケールは、アプリを作成した後は変更できません。

<a id="app.create.detail.univariate.sink"></a>
##### 結果転送 { #app.create.detail.univariate.sink }

検知結果を Prometheus に転送するための設定です。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 転送 URL | O | 結果を転送する宛先アドレス |
| API パス | O | 転送 URL の後に付加するパス。デフォルト /api/v1/write |
| スコアメトリクス名 | O | 異常スコアが保存されるメトリクス名。デフォルト AD_SCORE |
| しきい値メトリクス名 | X | 自動計算されたしきい値が保存されるメトリクス名。デフォルト AD_AUTO_THRESHOLD |

**[追加転送設定を展開]** をクリックすると、次の項目を設定できます。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 固定ヘッダ | X | 転送リクエストに常に付加する HTTP ヘッダ。認証トークンを設定する際に使用 |
| 動的ヘッダ | X | 結果レコードのフィールド値を HTTP ヘッダとして転送。受信側が要求する場合のみ使用 |
| メトリクスファミリー名 | X | スコアとしきい値のメトリクスをまとめて呼ぶ名前。空白にするとまとめない |

- 固定ヘッダは `ヘッダ名:値` の形式で入力し、複数ある場合はカンマで区切ります。例: `Authorization:Bearer abc123`
- 動的ヘッダは `レコードフィールド:ヘッダ名` の形式で入力し、複数ある場合はカンマで区切ります。例: `tenant:x-monitoring-tenant-alias`
- 同じヘッダ名や同じレコードフィールドを2回入力することはできません。形式が正しくない場合は、入力欄の下にエラーが表示されます。
- 同じ転送先アドレスに同じスコア・しきい値のメトリクス名を送信するアプリがすでに存在する場合、アプリを作成できません。結果の時系列が混在しないよう、メトリクス名または転送先アドレスを異なる値に指定してください。

!!! tip "注記"
    推論結果は Prometheus への転送とは別に、結果データソースに常に保存されます。結果データソースはアプリの作成時に自動的に作成され、分析メニューから照会できます。

<a id="app.create.review"></a>
#### 最終確認 { #app.create.review }

前の手順で入力した内容をアプリタイプに合わせて要約して表示します。

推薦システム:

| 確認項目 | 説明 |
| --- | --- |
| 基本設定 | アプリ名、説明、タイプ |
| モデル設定 | 選択したモデル、サービングリソース、バッチスケジュール、データ接続情報 |
| 追加設定 | スキルテーブルなどの追加設定 |

単変量異常検知:

| 検討項目 | 説明 |
| --- | --- |
| 基本設定 | アプリ名、説明、タイプ |
| 詳細設定 | 指標データソース、モデルリソース、再学習周期、検出オプション、結果転送設定 |

- 再学習周期を指定しなかった場合は、「指定なし（デフォルト周期）」と表示されます。
- 静的ヘッダと動的ヘッダは、入力した値の代わりに「設定済み」、「なし」、またはハイフンで表示されます。

**[保存]** ボタンをクリックするとアプリが作成されます。成功した場合は完了モーダルが表示されて一覧に移動し、失敗した場合はエラーメッセージが表示されます。

<a id="app.delete"></a>
### アプリの削除 { #app.delete }

1. 削除するアプリのチェックボックスを選択します。
2. **[削除]** ボタンをクリックします。
3. 確認モーダルで **[確認]** をクリックします。

!!! danger "注意"
    削除されたアプリは復元できません。接続されているサービングパイプラインも合わせて削除されます。

<a id="app.detail"></a>
### 推薦システムアプリ詳細 { #app.detail }

アプリ一覧でアプリをクリックすると、詳細画面に移動します。詳細画面はアプリのタイプによって異なり、推薦システムアプリは**推薦API呼び出し**、**アプリ情報**、**学習管理**の3つのタブで構成されます。

<a id="app.detail.recommend"></a>
#### 推薦API呼び出し { #app.detail.recommend }

推薦リクエストのパラメータを入力して推薦APIを直接呼び出し、結果を確認できます。画面は入力フォーム、リクエストプレビュー、推薦結果の3つの領域で構成されています。

入力フォーム:

| 項目 | 説明 |
| --- | --- |
| 推薦アプリID | 呼び出し対象のアプリID。現在のアプリが自動入力されます |
| ユーザーID | 推薦対象のユーザーを選択 |
| 推薦モード | Sequential（履歴ベース）、Cold Start（属性ベース）から選択 |
| 最大推薦数 | レスポンスに含める最大アイテム数（1〜100、デフォルト値10） |
| Longtail モード | 人気のないアイテムも含めて推薦の多様性を向上。Cold Startモードでは使用できません |
| context | 推薦リクエストのコンテキスト情報（現在・最近閲覧したアイテムなど）と行動シグナルをフィールド単位で追加 |
| userAttributes | ユーザー属性情報（グループ、年齢、関心事など）をフィールド単位で追加 |
| options | 推薦リクエストのオプションをフィールド単位で追加 |

- **リクエストプレビュー**: 入力値で構成された実際のAPIリクエストJSONを表示します。**[コピー]** ボタンでコピーしてAPI連携開発に活用できます。
- **推薦結果**: **[推薦リクエスト]** ボタンをクリックすると、順位、アイテムキー、スコアおよび推薦理由が表示され、総結果数とレスポンス時間も合わせて確認できます。

contextに以下のキーを追加することで、ユーザーの行動シグナルを入力できます。impressionsは、表示された推薦情報をもとに推薦結果を並び替えるために使用され、interactionsとfeedbackはユーザー行動ベースのデータをモデルの推論に反映します。

| キー | 説明 | タイプ |
| --- | --- | --- |
| impressions | ユーザーへのレコメンド結果として表示されたアイテムのリスト | なし |
| interactions | ユーザーがアイテムに対して行った行動情報 | CLICK, CONVERSION |
| feedback | ユーザーがアイテムに対して付けた評価 | POSITIVE, NEGATIVE |

**[項目追加]** ボタンで行を追加し、次の値を入力します。

| 項目 | 説明 |
| --- | --- |
| アイテムキー | 対象アイテムキー。impressionsは表示順序に従って複数入力 |
| タイプ | interactions、feedbackから選択 |
| requestId | 直前の推薦レスポンスのrequestId |
| 発生時刻 | タイムゾーンオフセットを含む ISO 8601 形式。例: 2026-08-25T10:00:00+09:00 |

impressionsは最大10件、interactionsとfeedbackはタイプ別に最大10件まで入力できます。上限を超えるとリクエストが拒否されます。

!!! tip "知っておくべきこと"
    推薦 API 呼び出しはアプリがアクティブ状態のときのみ可能です。
    impressions は推薦レスポンスを受け取るたびに自動で1件ずつ、最新10件まで蓄積されます。ユーザーを変更するか画面を更新すると初期化され、行の ✕ で1件ずつ、フィールドの ✕ で全件を削除できます。

<a id="app.detail.info"></a>
#### アプリ情報 { #app.detail.info }

アプリID、アプリ名、ステータス、アプリタイプ、説明、作成日、更新日、バージョン、および入出力データソースを確認できます。

- 入力データソース: モデルの学習に使用するデータソースです。推薦アプリはモデルごとに分けて表示されます。
- 出力データソース: 推薦結果が保存されるデータソースです。

<a id="app.detail.training"></a>
#### 学習管理 { #app.detail.training }

アプリに含まれる学習モデルの学習周期を変更し、自動再学習を停止・再開したり、学習を手動実行したりします。学習成果物の履歴もこのタブで確認できます。

| カラム | 説明 |
| --- | --- |
| 学習モデル | 学習対象のモデル |
| 学習ステータス | 学習待機、学習中、学習完了、再学習停止、学習失敗、削除済み |
| 学習周期 | 自動再学習が実行される周期 |
| 設定反映ステータス | 変更した学習設定の反映状況 |
| 自動再学習 | 使用中、停止済み、未設定 |
| 最終学習日時 | 最後に学習した日時 |
| 管理 | **[学習周期変更]** ボタン |

設定反映ステータス:

| 値 | 説明 |
| --- | --- |
| 確認前 | 反映状況をまだ確認していない |
| 反映中 | 変更した設定を反映中 |
| 反映済み | 変更した設定の反映が完了 |
| 反映失敗 | 反映に失敗。再試行が必要 |

上部のボタンで次の操作を実行します。操作はアプリに含まれるすべての学習モデルに適用されます。

| ボタン | 説明 |
| --- | --- |
| 学習実行 | 学習をすぐに実行 |
| 自動再学習停止 | 自動再学習を停止 |
| 自動再学習再開 | 停止した自動再学習を再開 |
| 更新 | 学習ステータスを再照会 |

ボタンが非アクティブ状態のときは、ボタンにマウスを合わせると理由を確認できます。

| 状況 | 案内 |
| --- | --- |
| 学習モデルがない | 学習モデルがありません。 |
| 学習が進行中 | 学習が進行中です。完了後に再度実行できます。 |
| 自動再学習が使用中 | 自動再学習を停止してから実行できます。 |
| 設定反映が完了していない | すべてのモデルの設定反映ステータスが「反映済み」である必要があります。 |
| 前回の変更が確認されていない | 前回の自動再学習の変更が確認待ちです。 |

**[学習周期変更]** ボタンをクリックすると、モーダルで周期を変更できます。

| 項目 | 必須 | 説明 |
| --- | --- | --- |
| 周期 | O | 毎日、毎週、時間間隔から選択して時刻を指定 |
| 変更理由 | X | 変更理由を入力 |

- 設定した時刻は、アクセスしているブラウザのタイムゾーンを基準に適用されます。
- 保存すると変更をリクエストした状態になり、反映状況は一覧の「設定反映ステータス」で確認できます。
- 一部のモデルのみ反映されていない場合は、該当モデルの一覧とともに案内が表示されます。

!!! tip "ヒント"
    学習実行は、自動再学習を停止した状態でのみ実行できます。
    学習周期変更モーダルで周期を変更せずに保存すると、「適用する変更はありません。」という案内が表示されます。

<a id="app.detail.training.history"></a>
##### 学習成果物履歴 { #app.detail.training.history }

一覧で学習モデルを選択すると、そのモデルの学習成果物履歴が表示されます。

| カラム | 説明 |
| --- | --- |
| バージョン | 学習成果物のバージョン |
| 使用ステータス | 現在サービング中の成果物は **サービング中**、最新の成果物は **最新学習版** と表示 |
| 学習日時 | 学習が完了した日時 |
| 実行方式 | 手動実行、自動再学習 |
| 学習結果 | リクエスト済み、準備中、送信済み、学習中、成功、失敗、キャンセルリクエスト済み、キャンセル済み |

まだ学習されたモデルがない場合は「まだ学習されたモデルがありません。」と表示されます。

<a id="app.detail.univariate"></a>
### 単変量異常検出アプリ詳細 { #app.detail.univariate }

単変量異常検出アプリは、上部に **アプリ > アプリ名** のパスが表示され、**アプリ情報**と**グループ一覧**の 2 つのタブで構成されます。アプリ一覧に戻るには、パスの **[アプリ]** をクリックします。

<a id="app.detail.univariate.info"></a>
#### アプリ情報 { #app.detail.univariate.info }

ヘッダーにアプリ名、ステータス、アプリタイプ、アプリID、作成日、変更日、説明が表示され、その下に**入力 → 処理 → 出力**の順にカードが表示されます。

**入力**: 指標データソース

| 項目 | 説明 |
| --- | --- |
| 名前 | 指標データソース名 |
| タイプ | データソースタイプ |
| グループキーフィールド | グループを分ける基準ラベル。データソースのグループラベル設定が表示され、指定していない場合は「設定なし（単一グループ）」 |
| シリーズ識別ラベル | 時系列を区別するラベル。指定していない場合は「全ラベルを使用」 |
| データソースID、データソーステーブル名 | 問い合わせやログ照合に使用 |

**処理**: 単変量異常検知モデル

| 項目 | 説明 |
| --- | --- |
| モデルリソース | 学習と推論に使用するCPU・メモリの上限 |
| デバイス | CPU または GPU |
| 学習状態 | 学習待機、学習中、学習完了、再学習中止、学習失敗、削除済み |
| 最終学習日 | 最後に学習した日時 |
| 再学習周期 | 「毎日 03:00」「毎週月曜日 03:00」「6時間ごと」の形式。指定していない場合は「設定なし」 |
| 転送モード | 正確モード、即時モード |
| スコアスケール | 元の値、または 0〜100 スケール |

**出力**: 結果転送

| 項目 | 説明 |
| --- | --- |
| 転送アドレス | 結果を転送するアドレス。転送URLとAPIパスを連結した値 |
| スコアメトリクス名 | 異常スコアが保存されるメトリクス名 |
| 基準値メトリクス名 | 異常判定の基準値が保存されるメトリクス名 |
| 結果データソース | 結果が保存されるデータソース名 |
| データソースID、データソーステーブル名 | 問い合わせやログ照合に使用 |

- 項目ラベル横の疑問符アイコンにマウスを合わせると、説明を確認できます。
- 転送アドレスを設定していないアプリには、「外部に送信せず、結果データソースにのみ保存します。」と表示されます。
- 静的ヘッダーと動的ヘッダーに入力した値は画面に表示されません。
- 推論結果は Prometheus 転送とは別に、結果データソースに常に保存され、分析メニューから照会できます。
- カードの下にはグループの状況が**全体**、**有効化**、**有効化待機中**、**無効化**の4つの数字で表示されます。数字をクリックすると、グループ一覧タブに移動し、該当ステータスでフィルタリングされます。
- 日時は接続したブラウザのタイムゾーンで表示されます。

<a id="app.detail.univariate.groups"></a>
#### グループリスト { #app.detail.univariate.groups }

異常検知は時系列ごとに行われ、グループはその時系列をまとめて結果と状態を確認する単位です。

| カラム | 説明 |
| --- | --- |
| グループキー | グループを分ける基準となるラベル名。グループキーフィールドがないアプリはハイフンで表示 |
| 値 | そのラベルの値。グループキーフィールドがないアプリは「単一グループ」 |
| グループハッシュ | グループを識別する16文字のハッシュ。グループキー値から自動的に計算 |
| 状態 | グループの現在の状態 |
| 作成日 | グループが登録された日時 |
| 変更日 | グループ情報が最後に変更された日時 |

状態:

| 値 | 説明 |
| --- | --- |
| 有効化 | このグループの検知結果が出力されている |
| 有効化待機中 | 有効になっているがデータを収集中のため、結果がまだ出力されていない |
| 無効化 | 使用しないように無効にしているグループ |

- データソースにグループラベルを指定すると、その値ごとにグループが1つ作成されます。指定しない場合は、データソース全体が1つのグループになります。
- グループはアプリを作成したときではなく、指標が届いた時点で登録されます。アプリを作成した直後はリストが空になっています。
- エラーはグループ単位で判定されます。グループ内の時系列が1つでも推論を停止すると、グループ全体がエラーになり、その時系列が回復して初めて正常に戻ります。
- ステータスでフィルタリングしたり、グループキーまたはグループハッシュで検索したりして、リストを絞り込むことができます。
- 1ページあたりの表示件数を変更できます（20件、50件、100件 / デフォルト20件）。
- 登録されたグループがない場合は「登録されたグループがありません。データが届いてグループが登録されると、ここに表示されます。」と表示され、検索・フィルタ条件に一致するグループがない場合は「条件に一致するグループがありません。」と表示されます。

<a id="app.detail.univariate.groups.hash"></a>
##### ハッシュ計算機 { #app.detail.univariate.groups.hash }

ツールバーの**ハッシュ計算機**ボタンをクリックすると、ラベルからハッシュを直接計算できます。

- ラベルの JSON をそのまま貼り付けるか、`名前=値` を1行に1つずつ入力します。
- 正規形と16文字のハッシュが表示され、**コピー**ボタンでコピーできます。
- グループハッシュはグループキーフィールドに該当するラベルのみ、シリーズハッシュは時系列を識別するラベルのみ入力します。
- まだ登録されていないグループのハッシュも事前に確認できます。
- ハッシュが想定と異なる場合は、まず正規形を照合します。正規形はラベルを名前順に並べ替えてカンマでつないだ値です。